---
self_signed_certificate:
  owner: "tomcat"
  group: "tomcat"
  certificate_directory: "/usr/share/tomcat/.postgresql"
  privatekey_filename: "{{ self_signed_certifcate.certificate_directory }}/postgresql.key"
  use_pk8: true
  pk8_filename: "{{ self_signed_certifcate.certificate_directory }}/postgresql.pk8"
  csr_filename: "{{ self_signed_certifcate.certificate_directory }}/postgresql.csr"
  csr_common_name: "{{ web_host }}"
  certificate_filename: "{{ self_signed_certifcate.certificate_directory }}/postgresql.crt"
  provider: "selfsigned"
  cache_filename: "{{ postgres_client_certificate_cache_filename }}"
