---
- name: Converge
  hosts: db
  become: true
  gather_facts: true
  tasks:
    - name: Create self-signed SSL certificates
      ansible.builtin.include_role:
        name: "mirsg.ssl_certificates"
